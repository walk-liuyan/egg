// compress 压缩
module.exports = require("koa-compress")

// 飞标准中间件
/* const Middleware = require('some-koa-middleware');
module.exports = (option, app) => {
  return Middleware(options.compiler, option.xxx)
} */